# WebWiz Node Health

## Features
- Provides system statistics including CPU, RAM, swap, and disk usage.
- Health check endpoint that returns "ALIVE".
- Configurable secret key for secure access to the statistics endpoint.
- Option to set up the server to start automatically on boot (Linux only).
